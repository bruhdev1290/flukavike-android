package com.fluxer.client.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Persona 5 Inspired Color Palette
 * Dark, high-contrast with dramatic red accents
 */

// Primary Colors - The iconic Persona 5 red
val PhantomRed = Color(0xFFE63946)
val PhantomRedDark = Color(0xFFB71C1C)
val PhantomRedLight = Color(0xFFFF6B6B)

// Background Colors - Deep blacks and dark grays
val VelvetBlack = Color(0xFF0D0D0D)
val VelvetDark = Color(0xFF141414)
val VelvetMid = Color(0xFF1A1A1A)
val VelvetLight = Color(0xFF252525)
val VelvetSurface = Color(0xFF2D2D2D)

// Accent Colors
val AlertYellow = Color(0xFFFFD93D)
val SuccessGreen = Color(0xFF4CAF50)
val InfoCyan = Color(0xFF00D9FF)
val WarningOrange = Color(0xFFFF9800)

// Text Colors
val TextPrimary = Color(0xFFFFFFFF)
val TextSecondary = Color(0xFFB3B3B3)
val TextMuted = Color(0xFF808080)
val TextOnRed = Color(0xFFFFFFFF)

// UI Element Colors
val BorderSharp = Color(0xFFE63946)
val BorderSubtle = Color(0xFF404040)
val BorderDark = Color(0xFF1A1A1A)

// Status Colors
val OnlineGreen = Color(0xFF43B581)
val AwayYellow = Color(0xFFFAA61A)
val DndRed = Color(0xFFF04747)
val OfflineGray = Color(0xFF747F8D)

// Gradient Colors
val GradientStart = Color(0xFF1A0A0A)
val GradientEnd = Color(0xFF0D0D0D)

// Special Effects
val GlowRed = Color(0xFFE63946).copy(alpha = 0.6f)
val ShadowHeavy = Color(0xFF000000).copy(alpha = 0.8f)
val OverlayDark = Color(0xFF000000).copy(alpha = 0.7f)

// Menu/Panel Colors
val PanelBackground = Color(0xFF141414)
val PanelBorder = Color(0xFFE63946).copy(alpha = 0.3f)
val SelectedItem = Color(0xFFE63946).copy(alpha = 0.15f)
val HoverItem = Color(0xFFFFFFFF).copy(alpha = 0.05f)
